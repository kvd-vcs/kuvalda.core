namespace Kuvalda.Core
{
    public class StatusOptions
    {
        public readonly string RepositoryPath;

        public StatusOptions(string repositoryPath)
        {
            RepositoryPath = repositoryPath;
        }
    }
}